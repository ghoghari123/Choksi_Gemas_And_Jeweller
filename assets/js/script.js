(() => {
    const page = document.body.dataset.page || 'home';
    const image = (url, alt) => `<img src="${url}" alt="${alt}" loading="lazy">`;
    const navItems = [['index.html', 'Home', 'home'], ['about.html', 'About Us', 'about'], ['jewellery.html', 'Jewellery', 'jewellery'], ['collections.html', 'Collections', 'collections'], ['craftsmanship.html', 'Craftsmanship', 'craftsmanship'], ['showroom.html', 'Showroom', 'showroom'], ['contact.html', 'Contact', 'contact']];
    const header = document.querySelector('#site-header');
    const footer = document.querySelector('#site-footer');
    if (header) header.innerHTML = `<div class="header-inner"><a class="brand" href="index.html"><img src="assets/images/logo.jpeg" alt="Choksi Gems & Jewellers"><span>EST. 1996 · SURAT</span></a><button class="menu-toggle" aria-expanded="false" aria-label="Open menu"><i class="fa-solid fa-bars"></i></button><nav class="nav-menu" aria-label="Main navigation">${navItems.map(([href, label, key]) => `<a class="${page === key ? 'active' : ''}" href="${href}">${label}</a>`).join('')}<a class="button" href="contact.html">Enquire Now</a></nav></div>`;
    if (footer) footer.innerHTML = `<div class="container footer-grid"><div class="footer-brand"><img src="assets/images/logo.jpeg" alt="Choksi Gems & Jewellers"><p class="footer-copy">Timeless jewellery, thoughtfully made for the moments that become family history.</p><div class="socials"><a href="https://instagram.com" aria-label="Instagram"><i class="fa-brands fa-instagram"></i></a><a href="https://www.indiamart.com/choksi-gems-jewellers/" aria-label="IndiaMART"><img class="indiamart-mark" src="assets/images/indiamart-logo.png" alt="IndiaMART"></a><a href="https://facebook.com" aria-label="Facebook"><i class="fa-brands fa-facebook-f"></i></a><a href="https://wa.me/919876543210" aria-label="WhatsApp"><i class="fa-brands fa-whatsapp"></i></a></div></div><div><h3 class="footer-title">Quick Links</h3><div class="footer-links">${navItems.slice(0, 5).map(([href, label]) => `<a href="${href}">${label}</a>`).join('')}</div></div><div><h3 class="footer-title">Explore</h3><div class="footer-links"><a href="jewellery.html#bridal">Bridal</a><a href="jewellery.html#gold">Gold</a><a href="jewellery.html#diamond">Diamond</a><a href="collections.html#traditional">Traditional</a><a href="collections.html#contemporary">Contemporary</a><a href="collections.html#daily">Daily Wear</a></div></div><div><h3 class="footer-title">Visit & Enquire</h3><div class="footer-links"><span>Ring Road, Surat, Gujarat</span><a href="tel:+919876543210">+91 98765 43210</a><a href="mailto:hello@choksigems.in">hello@choksigems.in</a><span>Mon - Sun · 10:30 am - 8:30 pm</span></div></div></div><div class="container footer-newsletter"><h3>Join Our Jewellery Journal</h3><form class="newsletter-form"><input type="email" name="email" placeholder="Enter your email address" aria-label="Email address" required><button type="submit" aria-label="Subscribe"><i class="fa-solid fa-arrow-right"></i></button></form></div><div class="container footer-meta"><span>© 2026 Choksi Gems & Jewellers. All Rights Reserved.</span><span><a href="#privacy" data-legal="privacy">Privacy Policy</a> &nbsp;·&nbsp; <a href="#terms" data-legal="terms">Terms & Conditions</a></span></div>`;
    const menuButton = document.querySelector('.menu-toggle');
    const navigation = document.querySelector('.nav-menu');
    const headerElement = document.querySelector('.site-header');
    const updateHeader = () => { headerElement?.classList.toggle('is-scrolled', window.scrollY > 24); document.querySelector('.back-top')?.classList.toggle('visible', window.scrollY > 500); };
    updateHeader(); window.addEventListener('scroll', updateHeader, { passive: true });
    menuButton?.addEventListener('click', () => { const open = navigation.classList.toggle('is-open'); headerElement.classList.toggle('menu-open', open); menuButton.setAttribute('aria-expanded', String(open)); menuButton.innerHTML = `<i class="fa-solid fa-${open ? 'xmark' : 'bars'}"></i>`; });
    navigation?.querySelectorAll('a').forEach(link => link.addEventListener('click', () => { navigation.classList.remove('is-open'); headerElement.classList.remove('menu-open'); }));
    const revealItems = document.querySelectorAll('.reveal, main section, .card, .feature, .process-item');
    revealItems.forEach((item, index) => { item.classList.add('reveal'); item.style.transitionDelay = `${Math.min(index % 6, 5) * 70}ms`; });
    if ('IntersectionObserver' in window) { const observer = new IntersectionObserver(entries => entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('is-visible'); observer.unobserve(entry.target); } }), { threshold: .1 }); revealItems.forEach(item => observer.observe(item)); } else revealItems.forEach(item => item.classList.add('is-visible'));
    document.querySelectorAll('img').forEach(img => img.addEventListener('error', () => { if (!img.dataset.fallback) { img.dataset.fallback = 'true'; img.src = 'assets/images/img.jpeg'; } }, { once: true }));
    const topButton = document.querySelector('.back-top'); topButton?.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
    const form = document.querySelector('#enquiry-form'); const formMessage = document.querySelector('.form-message');
    form?.addEventListener('submit', event => { event.preventDefault(); if (!form.checkValidity()) { form.reportValidity(); return; } form.reset(); formMessage?.classList.add('show'); });
    document.querySelectorAll('.newsletter-form').forEach(newsletter => newsletter.addEventListener('submit', event => { event.preventDefault(); const input = newsletter.querySelector('input'); const email = input.value.trim().toLowerCase(); const subscriptions = JSON.parse(localStorage.getItem('diamond_newsletter') || '[]'); if (!subscriptions.some(item => item.email === email)) subscriptions.push({ email, createdAt: new Date().toISOString() }); localStorage.setItem('diamond_newsletter', JSON.stringify(subscriptions)); let message = newsletter.querySelector('.newsletter-success'); if (!message) { message = document.createElement('span'); message.className = 'newsletter-success'; newsletter.appendChild(message); } message.textContent = 'Thank you for subscribing. Your email has been saved locally.'; message.classList.add('show'); input.value = ''; clearTimeout(newsletter.messageTimer); newsletter.messageTimer = setTimeout(() => message.classList.remove('show'), 2500); }));
    document.querySelectorAll('[data-legal]').forEach(link => link.addEventListener('click', event => { event.preventDefault(); const title = link.dataset.legal === 'privacy' ? 'Privacy Policy' : 'Terms & Conditions'; const modal = document.createElement('div'); modal.className = 'legal-modal is-open'; modal.innerHTML = `<div class="legal-panel"><button class="legal-close" aria-label="Close policy"><i class="fa-solid fa-xmark"></i></button><span class="section-label">CHOKSI GEMS & JEWELLERS</span><h2>${title}</h2><p>${title === 'Privacy Policy' ? 'Information shared through our enquiry, account and newsletter forms is used to respond to requests and provide our services. We do not sell personal information, and this static website stores demonstration form data only in your browser local storage.' : 'Website descriptions, images and educational material are provided for general information and may change. Product details, pricing, availability and grading documentation should be confirmed with Choksi Gems & Jewellers before purchase.'}</p></div>`; document.body.appendChild(modal); modal.querySelector('.legal-close').addEventListener('click', () => modal.remove()); modal.addEventListener('click', e => { if (e.target === modal) modal.remove(); }); }));
})();
